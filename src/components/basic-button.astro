---
const { text, url, className } = Astro.props;
---

<a href={url}
target="_blank">
    <button class="{className} bg-slate-900 hover:bg-slate-800 text-slate-100 font-bold py-2 px-4 rounded lg:px-5 lg:py-2.5 md:px-4 md:py-2
    hover:-translate-y-0.5 hover:scale-105 duration-300 w-full">
        {text}
    </button>
</a>