---
const { text, url, className } = Astro.props;
---

<a href={url}>
    <button class="{className} bg-slate-900 hover:bg-slate-700 text-slate-100 font-bold py-2 px-4 rounded">
        {text}
    </button>
</a>