---
const { text, url, className } = Astro.props;
---

<a href={url}
target="_blank">
    <button class="{className} bg-slate-50 hover:bg-slate-200 border-2 border-black text-slate-900 py-2 px-4 rounded lg:px-5 lg:py-2.5 md:px-4 md:py-2
    w-full">
        {text}
    </button>
</a>